import {Rule} from './lotteries'
function overlap(a:number[],b:number[]){const s=new Set(a);return b.filter(n=>s.has(n)).length}
function scoreStructure(nums:number[],rule:Rule){const odd=nums.filter(n=>n%2).length;const evenBal=1-Math.abs(odd-nums.length/2)/(nums.length/2||1);const zones=[0,0,0,0];nums.forEach(n=>zones[Math.min(3,Math.floor((n-1)/(rule.universe/4)))]++);const ideal=nums.length/4;const zone=1-zones.reduce((s,c)=>s+Math.abs(c-ideal),0)/(nums.length*1.5);return Math.max(0,.6*evenBal+.4*zone)}
function randomSet(rule:Rule,picks:number){const arr=Array.from({length:rule.universe},(_,i)=>i+1);for(let i=arr.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[arr[i],arr[j]]=[arr[j],arr[i]]}return arr.slice(0,picks).sort((a,b)=>a-b)}
export function generatePortfolio(rule:Rule,picks:number,count=4){const games:number[][]=[];for(let g=0;g<count;g++){let best:number[]=[],bestScore=-1e9;for(let i=0;i<1400;i++){const c=randomSet(rule,picks);const maxOv=games.length?Math.max(...games.map(x=>overlap(x,c))):0;const unique=c.filter(n=>!games.flat().includes(n)).length/picks;const s=unique*.5+scoreStructure(c,rule)*.35-(maxOv/picks)*.5+Math.random()*.02;if(s>bestScore){bestScore=s;best=c}}games.push(best)}return games}
export function metrics(games:number[][]){const flat=games.flat();const unique=new Set(flat).size;let pairs=0,total=0;for(let i=0;i<games.length;i++)for(let j=i+1;j<games.length;j++){pairs++;total+=overlap(games[i],games[j])}return{unique,overlap:pairs?Number((total/pairs).toFixed(2)):0,coverage:Number((unique/flat.length*100).toFixed(1))}}
